# Built binaries (root-anchored so they never match the cmd/ledger SOURCE
# directory — an unanchored "ledger" would).
/ledger
/server
/bin/
*.exe

# Test / coverage
*.out
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/
